# Base de datos
DB_USER=
DB_PASSWORD=
DB_NAME=

# Backend
ASPNETCORE_ENVIRONMENT=

# Super admin (se crea al iniciar la API si no existe)
SUPER_ADMIN_EMAIL=superadmin@elohim.gt
SUPER_ADMIN_PASSWORD=SuperAdmin123!
SUPER_ADMIN_NOMBRE=Super Admin

# Backend — datos de prueba al arrancar (productos, ventas, usuarios demo)
# Ver también backend/.env.example
SEED_DATA=true

# Stripe (backend — no exponer secretos al frontend)
# STRIPE_SECRET_KEY=
# STRIPE_PUBLISHABLE_KEY=
# STRIPE_WEBHOOK_SECRET=   # signing secret del endpoint /api/pagos/webhook
# STRIPE_DEFAULT_CURRENCY=gtq

# Frontend
NEXT_PUBLIC_API_URL=
BACKEND_API_URL=